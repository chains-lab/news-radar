type: object
required:
  - type
  - section_id
properties:
  type:
    type: string
    enum: [ content_update_section ]
  section_id:
    type: integer
    description: Unique identifier for the content section to be deleted.